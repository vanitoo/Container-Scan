# Ruff TODO

Список оставшихся замечаний после безопасной чистки GUI.
Файл нужен, чтобы не пытаться чинить весь долг за один проход.

## 2026-06-30

По результатам `ruff check .` в проекте остаются старые замечания в нескольких модулях:

- `app.py`: сортировка импортов, bare `except`, отсутствие финальной новой строки.
- `config.py`: сортировка импортов, неиспользуемый `os`, отсутствие финальной новой строки.
- `gui/main_window.py`: старые локальные замечания `F841`, `E501`, `F821`.
- `gui/themes.py`: сортировка импортов, неиспользуемый `tkinter`, неиспользуемая `ACCENT`, отсутствие финальной новой строки.
- `models/state.py`: переход на современные аннотации `list` / `dict` / `X | None`.
- `run.py`: сортировка импортов, перенос на `pathlib`, отсутствие финальной новой строки.
- `services/excel_service.py`: сортировка импортов.
- `services/matching_service.py`: сортировка импортов, неиспользуемая переменная цикла, отсутствие финальной новой строки.
- `services/ocr_service.py`: сортировка импортов, `Optional`-аннотация, `pathlib`-замены, отсутствие финальной новой строки.
- `services/pdf_service.py`: сортировка импортов, `RUF046` на лишние `int`, связанные с координатами.
- `utils/file_utils.py`: сортировка импортов, `EM102`, отсутствие финальной новой строки.
- `utils/helpers.py`: сортировка импортов, отсутствие финальной новой строки.
- `utils/logger.py`: сортировка импортов, `RUF012` по `COLORS`, отсутствие финальной новой строки.
- `utils/updater.py`: сортировка импортов, неиспользуемый `logging`, длинная строка, ошибки с `e` в lambda, отсутствие финальной новой строки.

## Дальше

Чинить это по одному модулю за раз, начиная с самых маленьких и безопасных правок.
